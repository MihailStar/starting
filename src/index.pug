extends ./templates/_base.pug

block variables
  - const pageTitle = 'Стартовый шаблон для верстки'
  - const bodyClass = 'body body_index'

block head
  meta(name='robots' content='none')

block body
  h1= pageTitle